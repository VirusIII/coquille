<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sevsu.ru.coquille">

    <permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PoolThingActivity"></activity>
        <activity android:name=".PollingActivity" />
        <activity
            android:name=".DigestThingActivity"
            android:label="@string/title_activity_digest_thing"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DigestActivity"
            android:label="@string/title_activity_digest"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MapActivity"
            android:label="@string/map" />
        <activity android:name=".NonActivity" />
        <activity
            android:name=".AboutActivity"
            android:label="@string/about_page" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>